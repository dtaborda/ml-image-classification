# Pillow 10.4.0 es la última versión compatible con Python 3.8
Pillow==10.4.0
pytest==8.3.4
redis==5.2.0
# TensorFlow 2.10+ es la primera versión compatible con Apple Silicon (ARM64)
# Versión 2.13 es estable y compatible con el código existente
tensorflow==2.13.0
# Protobuf compatible con TensorFlow 2.13
protobuf==3.20.3